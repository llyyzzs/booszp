<view class="container">
  <view class="content">
    <block wx:for="{{ITEM}}" wx:key="id">
      <view class="item" bindtap="tiaozhuan" data-id="{{item.job.id}}">
        <view class="item-1">
          <view class="item-1-1">{{item.job.name}}</view>
          <view class="item-1-2">{{item.job.price}}</view>
        </view>
        <view class="item-2">{{item.job.company.name}}<text decode="{{true}}">&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</text>{{item.job.company.scale}}</view>
        <view class="item-3">
          <block wx:for="{{item.job.tags}}" wx:key="id"><text>{{item}}</text></block>
        </view>
        <view class="item-4">
          <view class="item-4-1">
            <image src="{{item.job.image}}" mode="aspectFill"></image><text decode="{{true}}">&nbsp;&nbsp;&nbsp;{{item.job.mz}} {{item.job.sf}}</text>
          </view>
          <view class="item-4-2">{{item.job.position.city}}</view>
        </view>
      </view>
    </block>
  </view>
</view>