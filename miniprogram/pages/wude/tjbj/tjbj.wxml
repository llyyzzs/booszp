<view class="container">
  <form bindsubmit="onSubmit">
    <view class="content">
    <input type="text" placeholder="标题" name="title" value="{{note.title}}"/>
    <textarea name="content" placeholder="内容" value="{{note.content}}" maxlength=""></textarea></view>
    <button form-type='submit'>保存</button>
  </form>
</view>
